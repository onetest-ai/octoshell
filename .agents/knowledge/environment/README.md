# environment/

Local setup and the dev loop: the Extension Development Host (F5), `watch:host`/`watch:webview`,
anything that trips up a fresh clone. `CLAUDE.md` § Commands already covers the day-one commands —
a note here should add something beyond that (a gotcha, a non-obvious prerequisite), not restate
the command list.

**Not here:** one-off machine issues that aren't reproducible by another engineer.

## Index

_Empty — no notes yet._
