# integrations/

External systems this repo depends on: the VS Code extension API surfaces actually used, GitHub
Actions (CI), and (once built) the `octograph` bridge described in
`docs/superpowers/specs/2026-08-09-octograph-design.md`.

**Not here:** internal package-to-package coupling — that's `../architecture/`.

## Index

_Empty — no notes yet. Candidate for a first note once verified: which VS Code API surfaces
`board-watcher` and `EntityPanelManager` actually rely on, and any version-pinning reason behind
them._
